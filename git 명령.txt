git 명령

dir:파일 보여주기

cls:말끔히 정리하기

cd 폴더명: 상위폴더명으로 이동

git init:내가 만든 서버와 git을 연결

git add *: git init으로 연결한 모든 파일

git commit -m"first commit": 버전 만들기

※깃허브
git cmd에서 git init으로 연결하기
코딩작업하기
github 에 들어가서 기억만들기